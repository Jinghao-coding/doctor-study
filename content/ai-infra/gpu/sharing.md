GPU 共享方式不要混在一起背，核心是区分隔离粒度和调度目标。

- MIG：硬件切片，隔离强，适合多租户推理和强隔离场景。
- MPS：软件复用，多个进程共享 GPU 上下文，适合互补 workload，但干扰风险更高。
- 时间片：按时间轮转上下文，适合轻量任务和交互式场景。
- CUDA VMM：虚拟地址与物理显存分离，适合 KV 缓存这类峰值弹性大的内存管理。

面试追问通常会落到「为什么 MIG 更安全但利用率不一定最高」：因为 MIG 的资源边界是静态硬切分，降低干扰的同时也牺牲了跨分区弹性。
