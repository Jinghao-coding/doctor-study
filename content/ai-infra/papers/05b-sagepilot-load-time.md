## 用过去的重新加载耗时估计下一次

加载时间的 serving 校准来自真实日志：同一个模型过去在同类 GPU 上重新加载用了多久，就用这些记录估计下一次的成本。日志读取 `model_load_finished.payload.duration_sec`。这项统计可直接替换 GNN 资源表里的原始部署时间预测。

```text
首次加载 Q8 → 使用 → 回收 Q8 的 GPU 副本 → 再次加载 Q8 → 使用
                                        ↑ warm reload 统计对象
```

每轮运行内，按 `(model_key, GPU 类型)` 识别首次加载并排除，后续记录进入 warm 统计。这是按事件顺序划分的操作口径；它不直接检查操作系统缓存，不能仅凭“第几次”证明缓存一定热。缓存仍在主机侧时可能更快，但存储拥堵和并发加载也会改变耗时。

初稿 §3.3 只定义部署级加载代价与缓存接口，未展开 warm reload 的两层中位数。这套统计和门槛来自代码后处理路径，不能将其当成初稿公式，也不能仅凭初稿确认 Table 1 的加载误差统计覆盖了该校准。

## 两层中位数算例

下面全部是演示数字，不是论文实验结果。

<div class="table-scroll">

| 运行轮次 | 首次加载，排除 | 后续 reload 耗时 | 该轮中位数 |
|---|---|---|---|
| 第 1 轮 | 90 秒 | 28、30、32 秒 | 30 秒 |
| 第 2 轮 | 85 秒 | 29、31、33 秒 | 31 秒 |
| 第 3 轮 | 100 秒 | 27、29、35 秒 | 29 秒 |

</div>

每轮先排序取中间值，然后对 `30、31、29` 再取中位数，得到 30 秒。下一次同模型在同类 GPU 上重新加载时，资源表提供的经验估计就是 30 秒。

\[
\hat T_{load}=\operatorname{median}_{run}\left(\operatorname{median}(T_{warm\ reload\ in\ run})\right)
\]

先按轮汇总，使每个有 warm 记录的运行贡献一个代表值，避免某轮因重复加载特别多而主导总体统计。排除首次加载按部署身份进行，直接替换原值的最终统计范围是“模型名 × GPU 类型”。

## 数据不足如何回退

<div class="table-scroll">

| 条件 | 采用的估计 |
|---|---|
| 模型 × GPU 至少有 3 轮 warm 记录、合计至少 5 条 warm 样本 | 使用两层中位数替换原值 |
| 该组样本不足，但同类 GPU 有满足门槛的模型 | 使用 GPU 级经验比例乘原估计 |
| 连 GPU 级比例也无法取得 | 保留原资源表的加载时间 |

</div>

GPU 比例由合格模型的 `warm 实测代表值 / 原估计值` 取中位数。例如其他合格模型的代表比例为 0.5，样本不足模型原来估计 80 秒，回退值为 40 秒。这仍是跨模型的粗略校准。

## 调度怎样使用 30 秒

假设任务预计第 100 秒 ready，加载估计 30 秒，预取余量 5 秒，则预取资格从第 `100 - 30 - 5 = 65` 秒开始。若资源允许并及时发起，加载可与前驱执行重叠。实际策略仍先推进 ready 工作，所以到第 65 秒不意味着一定加载。

同一加载成本也参与回收决策：预计 100 秒后再用且重加载只需 20 秒的模型，比 30 秒后再用且重加载需 25 秒的模型更适合暂时回收，前提是其他保护条件相同。

<div class="qa" onclick="this.classList.toggle('open')">
<div class="qa-q">Q: 没有神经网络参与，为什么也叫预测？</div>
<div class="qa-a"><p>预测是给尚未发生的事件提供估计。历史中位数本身就是统计预测器：把可比较的过去测量汇总，作为下一次的预计耗时。GNN 可以给原始值，历史数据足够时再用 serving 的真实重加载统计替换。</p></div>
</div>

<div class="qa" onclick="this.classList.toggle('open')">
<div class="qa-q">Q: 这 30 秒能用来估计首次冷启动吗？</div>
<div class="qa-a"><p>不能直接等同。统计特意排除了每轮首次加载，目标是后续重加载成本。首次冷启动的文件读取、缓存与引擎初始化状态可能不同，需要单独的观测口径或估计。</p></div>
</div>

核对依据：`src/experiment/workflow/cache_postprocess.py` 中 `_robust_medians`、`_gpu_scale` 与 `_calibrated_load`；门槛分别是 `MIN_CALIBRATION_RUNS=3` 和 `MIN_WARM_SAMPLES=5`。
