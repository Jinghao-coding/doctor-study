[project]
name = "doctor-study"
version = "0.1.0"
description = "Static study site generator for interview and project preparation."
requires-python = ">=3.13"
dependencies = []

[tool.uv]
package = false
